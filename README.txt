If you are using any IDE, run Price.java which has the main class. If you want to change the period value or times 
between the tasks execution. Change the second and third paramters of ScheduleAtFixedInterval(1,2,3).
After running the program, Enter a pair value and target value.